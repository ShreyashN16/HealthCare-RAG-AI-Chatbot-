# =============================================
# Healthcare RAG Chatbot - CPU-Only Dependencies
# =============================================
# Install with:
#   pip install --no-cache-dir -r requirements.txt

# --- CPU-only PyTorch (MUST be installed first) ---
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision
torchaudio

# --- Core framework ---
Flask==3.1.1
gunicorn==23.0.0
Werkzeug==3.1.3

# --- AI/ML - RAG pipeline ---
sentence-transformers>=3.0.0,<5.0.0
faiss-cpu>=1.7.0
numpy>=1.26.0,<2.1.0

# --- PDF processing ---
pdfplumber==0.11.6

python-dotenv